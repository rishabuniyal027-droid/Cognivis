apiVersion: v1
kind: Service
metadata:
  name: {{ include "Cognivis.fullname" . }}
  labels:
    {{- include "Cognivis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Cognivis.selectorLabels" . | nindent 4 }}
