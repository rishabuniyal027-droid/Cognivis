apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "Cognivis.labels" . | nindent 4 }}
  name: {{ include "Cognivis.fullname" . }}-config
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}